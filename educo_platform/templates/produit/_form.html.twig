{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

<div class="card shadow-sm p-4 mt-4">

    <div class="mb-3">
        {{ form_label(form.nom, 'Nom du produit', {'label_attr': {'class': 'form-label fw-bold '}}) }}
        {{ form_widget(form.nom, {
            'attr': {
                'class': form.nom.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control',
                'placeholder': 'Saisir le nom du produit'
            }
        }) }}
        <div class="invalid-feedback">
            {{ form_errors(form.nom) }}
        </div>
    </div>

    <div class="mb-3">
        {{ form_label(form.description, 'Description du produit', {'label_attr': {'class': 'form-label fw-bold '}}) }}
        {{ form_widget(form.description, {
            'attr': {
                'class': form.description.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control',
                'placeholder': 'Décrire le produit'
            }
        }) }}
        <div class="invalid-feedback">
            {{ form_errors(form.description) }}
        </div>
    </div>

    <div class="mb-3">
        {{ form_label(form.prix, 'Prix du produit', {'label_attr': {'class': 'form-label fw-bold '}}) }}
        {{ form_widget(form.prix, {
            'attr': {
                'class': form.prix.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control',
                'placeholder': 'Saisir le prix'
            }
        }) }}
        <div class="invalid-feedback">
            {{ form_errors(form.prix) }}
        </div>
    </div>

    <div class="mb-3">
        {{ form_label(form.stock, 'Quantité en stock', {'label_attr': {'class': 'form-label fw-bold '}}) }}
        {{ form_widget(form.stock, {
            'attr': {
                'class': form.stock.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control',
                'placeholder': 'Saisir la quantité'
            }
        }) }}
        <div class="invalid-feedback">
            {{ form_errors(form.stock) }}
        </div>
    </div>

    <div class="mb-3">
        {{ form_label(form.imageFile, 'Image du produit', {'label_attr': {'class': 'form-label fw-bold '}}) }}
        {{ form_widget(form.imageFile, {
            'attr': {
                'class': form.imageFile.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control',
                'placeholder': 'Choisir une image'
            }
        }) }}
        <div class="invalid-feedback">
            {{ form_errors(form.imageFile) }}
        </div>
    </div>

    <div class="mb-3">
        {{ form_label(form.categorie, 'Catégorie du produit', {'label_attr': {'class': 'form-label fw-bold '}}) }}
        {{ form_widget(form.categorie, {
            'attr': {
                'class': form.categorie.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control'
            }
        }) }}
        <div class="invalid-feedback">
            {{ form_errors(form.categorie) }}
        </div>
    </div>

    <div class="d-flex justify-content-between">
        <a href="{{ path('app_produit_index') }}" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left"></i> Retour à la liste
        </a>
        <button type="submit" class="btn btn-primary">
            <i class="fas fa-save"></i> {{ button_label|default('Enregistrer') }}
        </button>
    </div>

</div>

{{ form_end(form) }}
