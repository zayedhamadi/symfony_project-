{% extends 'side-bar.html.twig' %}

{% block title %}Modifier l'Élève{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Modifier l'Élève</h1>

    <div class="card p-4">
        {{ include('eleve/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mt-3">
        <a href="{{ path('app_eleve_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>

    {# <div class="mt-3">
        {{ include('eleve/_delete_form.html.twig') }}
    </div> #}
</div>
{% endblock %}