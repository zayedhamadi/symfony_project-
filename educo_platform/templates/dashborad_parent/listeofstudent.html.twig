{# {% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
{% endblock %}

{% block body %}
<div class="container mt-5">
    <h2>Liste des Élèves</h2>
    <div class="row mt-4">
        {% for eleve in eleves %}
            <div class="col-md-4">
                <div class="card mb-4">
                    <img src="https://randomuser.me/api/portraits/men/75.jpg" class="card-img-top" alt="Photo de profil">
                    <div class="card-body">
                        <h5 class="card-title">{{ eleve.nom }} {{ eleve.prenom }}</h5>
                        <p class="card-text"><strong>ID :</strong> {{ eleve.id }}</p>
                        <p class="card-text"><strong>Classe :</strong> {{ eleve.classe }}</p>
                        <a href="{{ path('app_dashboard_parent_eleve_details', {'id': eleve.id}) }}" class="btn btn-info">Détails</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %} #}
{% extends 'side-bar-parent.html.twig' %}
{% block title %}Liste des Élèves{% endblock %}

{% block body %}
   <div class="container mt-5">
    <h2>Liste des Élèves</h2>
    <div class="row mt-4">
        {% for eleve in eleves %}
            <div class="col-md-4">
                <div class="card mb-4">
                   
                    <div class="card-body">
                        <h5 class="card-title">{{ eleve.nom }} {{ eleve.prenom }}</h5>
                        <p class="card-text"><strong>ID :</strong> {{ eleve.id }}</p>
                        <p class="card-text"><strong>Classe :</strong> {{ eleve.Idclasse.NomClasse }}</p> <!-- Assurez-vous que 'nomClasse' est le bon attribut dans l'entité Classe -->
                        <a href="{{ path('app_dashboard_parent_eleve_details', {'id': eleve.id}) }}" class="btn btn-info">Détails</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>

{% endblock %}