{% extends 'side-bar.html.twig' %}

{% block title %}Nouvelle Classe{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Créer une nouvelle classe</h1>

    <div class="card p-4">
        {{ include('gestion_classe/_form.html.twig') }}
    </div>

    <div class="mt-3">
        <a href="{{ path('app_gestion_classe_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>
</div>
{% endblock %}