{# {% extends 'side-bar-parent.html.twig' %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .profile-card {
            max-width: 400px;
            margin: 50px auto;
            text-align: center;
            padding: 30px;
            background: white;
            border-radius: 15px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .profile-card img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            border: 4px solid #007bff;
        }
    </style>
{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="profile-card">
        <img src="https://randomuser.me/api/portraits/men/75.jpg" alt="Photo de profil">
        <h3 class="mt-3">Jean Dupont</h3>
        <p class="text-muted">Développeur Web | Passionné de Technologie</p>

        <div class="mb-3">
            <p><strong>Email :</strong> jean.dupont@email.com</p>
            <p><strong>Localisation :</strong> Paris, France</p>
            <p><strong>Inscrit depuis :</strong> 2022</p>
        </div>

        <div class="d-flex justify-content-center">
            <button class="btn btn-primary me-2">Modifier</button>
            <button class="btn btn-outline-secondary">Suivre</button>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %} #}
{% extends 'side-bar-parent.html.twig' %}

{% block title %}Tableau de bord du Parent{% endblock %}

{% block body %}
    <div class="container">
        <h1>Bonjour, {{ user.getPrenom() }} {{ user.getNom() }} !</h1>
        <p>Voici votre profil :</p>

        <ul>
            <li><strong>Nom complet :</strong> {{ user.prenom }} {{ user.nom }}</li>
            <li><strong>Email :</strong> {{ user.email }}</li>
            <li><strong>Rôle :</strong> Parent</li>
            <!-- Ajoute d'autres informations du parent ici -->
        </ul>

        <a href="{{ path('app_dashboard_parent_liste_eleves') }}" class="btn btn-primary">Voir la liste des élèves</a>
    </div>
{% endblock %}