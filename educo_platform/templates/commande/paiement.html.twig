{% extends 'base.html.twig' %}

{% block title %}Choix du Paiement{% endblock %}

{% block body %}
    <div class="container" style="margin-top: 100px !important; padding: 0 15px !important;">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-lg border-0 rounded-4">
                    <div class="card-body text-center p-5">
                        <h2 class="mb-4 fw-bold">Choisissez votre mode de paiement</h2>

                        <div class="d-flex justify-content-center gap-4">
                            <form action="/commander-et-payer" method="POST" data-turbo="false">
                                <button type="submit" class="btn btn-primary btn-lg d-flex align-items-center gap-2 px-4">
                                    <i class="fas fa-credit-card"></i> Payer par Carte
                                </button>
                            </form>

                            <a href="{{ path('app_paiement_livraison') }}" class="btn btn-secondary btn-lg d-flex align-items-center gap-2 px-4">
                                <i class="fas fa-university"></i> Payer à l'Administration
                            </a>
                        </div>

                        <div class="mt-4">
                            <a href="{{ path('app_panier_view') }}" class="btn btn-outline-warning btn-lg d-flex align-items-center gap-2">
                                <i class="fas fa-arrow-left"></i> Retour au Panier
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
