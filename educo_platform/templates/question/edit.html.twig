{% extends 'side-bar.html.twig' %}

{% block title %}Modifier la Question{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Modifier la Question</h1>

        <div class="card p-4 shadow-sm">
            {{ include('question/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>

        <a href="{{ path('question_index') }}" class="btn btn-secondary mt-3">
            Retour à la liste
        </a>
    </div>
{% endblock %}
