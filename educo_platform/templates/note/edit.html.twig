{% extends 'side-bar.html.twig' %}

{% block title %}Modifier la Note{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Modifier la Note</h1>

        <div class="card p-4 shadow-sm">
            {{ include('note/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>

        <a href="{{ path('note_index') }}" class="btn btn-secondary mt-3">
            Retour à la liste
        </a>
    </div>
{% endblock %}
